## Описание проекта

Telegram‑бот для изучения слов иностранного языка. Бот не требует отдельного сайта: всё общение происходит прямо в Telegram.  
Пользователь может добавлять свои слова или брать готовый набор, получать напоминания о повторении, проходить тренировки по карточкам и смотреть статистику прогресса.

## Архитектура и технология

- **Технологии**
  - Python 3.11+
  - Telegram‑бот на `aiogram 3.13`
  - FastAPI для REST API
  - База данных: PostgreSQL
  - Фоновые задачи для напоминаний о повторении слов

- **Компоненты приложения**
  - **Telegram‑бот**: обработка команд, сообщений и сценариев общения
  - **FastAPI**: REST API для работы с данными
  - **Модуль работы с БД**: взаимодействие с PostgreSQL через SQLAlchemy (async)
  - **Модуль бизнес‑логики**: словарь, тренировки, подсчёт прогресса

- **Инфраструктура**
  - Приложение и база данных поднимаются через `docker-compose`
  - Три контейнера: база данных, бот и API

## Установка и запуск

### Требования

- Python 3.11 или выше
- PostgreSQL 15+ (или использовать Docker)
- Docker и Docker Compose (опционально, для удобного запуска)

### Шаг 1: Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd final_project_python
```

### Шаг 2: Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл и укажите:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/English_bot
```

**Как получить BOT_TOKEN:**
1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

### Шаг 5: Настройка базы данных

#### Вариант А: Использование Docker (рекомендуется)

```bash
docker-compose up -d db
```

База данных будет доступна на `localhost:5432`

#### Вариант Б: Локальная установка PostgreSQL

1. Установите PostgreSQL
2. Создайте базу данных:
```sql
CREATE DATABASE English_bot;
```
3. Укажите правильные credentials в `.env`

### Шаг 6: Запуск приложения

#### Запуск Telegram-бота

```bash
python -m bot.main
```

#### Запуск FastAPI сервера (в отдельном терминале)

```bash
uvicorn api.main:app --reload
```

API будет доступно на `http://localhost:8000`
Документация API: `http://localhost:8000/docs`

### Запуск через Docker Compose (весь проект сразу)

```bash
docker-compose up --build
```

Это запустит:
- PostgreSQL базу данных
- Telegram-бота
- FastAPI сервер

## Структура проекта

```
final_project_python/
├── api/                    # FastAPI приложение
│   ├── __init__.py
│   ├── main.py            # Основной файл API
│   ├── config.py          # Конфигурация API
│   ├── database.py        # Настройка БД для API
│   └── schemas.py         # Pydantic схемы
├── bot/                    # Telegram-бот
│   ├── __init__.py
│   ├── main.py            # Точка входа бота
│   ├── config.py          # Конфигурация бота
│   ├── database.py        # Настройка БД для бота
│   ├── core/              # Ядро бота
│   │   ├── keyboards.py   # Клавиатуры
│   │   ├── states.py      # FSM состояния
│   │   └── texts.py       # Тексты сообщений
│   ├── handlers/          # Обработчики команд
│   │   ├── start.py       # Команда /start
│   │   └── menu.py        # Главное меню
│   └── models/            # Модели БД
│       └── db_models.py   # SQLAlchemy модели
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile             # Dockerfile для приложения
├── requirements.txt       # Зависимости Python
├── .env.example          # Пример файла с переменными окружения
└── README.md             # Этот файл
```

## Использование

### Команды бота

- `/start` - Начать работу с ботом
- `➕ Добавить слова` - Добавить новое слово в формате `слово — перевод`
- `📚 Тренировка` - Начать тренировку (в разработке)
- `🃏 Колоды` - Выбрать готовую колоду слов
- `📊 Прогресс` - Посмотреть статистику обучения
- `⚙️ Настройки` - Настройки бота (в разработке)

### API Endpoints

Основные endpoints API:

- `GET /user/{telegram_id}` - Получить пользователя
- `POST /user/{telegram_id}` - Создать пользователя
- `GET /deck/{deck_id}` - Получить колоду
- `POST /deck` - Создать колоду
- `GET /word/{word_id}` - Получить слово
- `POST /word` - Создать слово
- `GET /user_word_stats/{user_id}/{word_id}` - Получить статистику
- `POST /user_word_stats` - Создать/обновить статистику

Полная документация доступна по адресу `http://localhost:8000/docs`

## Разработка

### Добавление новых обработчиков

1. Создайте файл в `bot/handlers/`
2. Создайте router: `router = Router(name="название")`
3. Добавьте обработчики с декораторами `@router.message()` или `@router.callback_query()`
4. Подключите router в `bot/main.py`: `dp.include_router(ваш_router)`

### Работа с базой данных

Все модели находятся в `bot/models/db_models.py`. Используйте async сессии:

```python
from bot.database import get_session
from bot.models.db_models import User

async for session in get_session():
    result = await session.execute(select(User))
    users = result.scalars().all()
```

## Основной функционал (MVP)

- ✅ Регистрация пользователя
- ✅ Добавление слов пользователем
- ✅ Хранение слов и прогресса
- ✅ REST API для работы с данными
- 🚧 Режим тренировки с карточками (в разработке)
- 🚧 Напоминания о повторении (в разработке)
- 🚧 Просмотр статистики с реальными данными (в разработке)

## Возможные направления развития

- Продвинутый алгоритм интервального повторения (Spaced Repetition)
- Обучение через контент (фильмы, сериалы, книги)
- Диалоги-персонажи для практики
- Новые режимы обучения (грамматика, аудирование)
- Режим подготовки к экзаменам (IELTS, Goethe и др.)

## Лицензия

Этот проект создан в учебных целях.
